image: node:10.3.0

pages:
  stage: deploy
  script:
  - npm install
  - npm run lint
  - npm run build
  - mkdir .public
  - cp -r * .public
  - mv .public public
  artifacts:
    paths:
    - public
  only:
  - master
