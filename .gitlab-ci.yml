image: node:10.3.0

build:
  script:
  - npm install
  - npm run lint
  - npm run build

pages:
  stage: deploy
  script:
  - mkdir .public
  - cp -r * .public
  - mv .public public
  artifacts:
    paths:
    - public
  only:
  - master
